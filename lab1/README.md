# Lab 1: Referential Integrity Constraints

## 📚 รายละเอียด Lab

Lab นี้เป็นการศึกษาเกี่ยวกับ **Referential Integrity Constraints** (ข้อจำกัดความถูกต้องเชิงอ้างอิง) ในฐานข้อมูลเชิงสัมพันธ์ โดยใช้ PostgreSQL เป็นระบบฐานข้อมูล

### เนื้อหาที่ครอบคลุม

- Primary Key (PK) และ Foreign Key (FK)
- พฤติกรรมของ Foreign Key Constraints 3 แบบ:
  1. **NO ACTION** - ป้องกันการลบ/อัปเดตถ้ายังมีการอ้างอิง
  2. **SET NULL** - ตั้งค่า FK เป็น NULL เมื่อแถวแม่ถูกลบ/อัปเดต
  3. **CASCADE** - ลบ/อัปเดตแถวลูกตามแถวแม่โดยอัตโนมัติ

## 🎯 วัตถุประสงค์

1. เข้าใจความสัมพันธ์ระหว่างตารางผ่าน Primary Key และ Foreign Key
2. เรียนรู้พฤติกรรมต่างๆ ของ Foreign Key Constraints
3. ทดลองการลบและอัปเดตข้อมูลในตารางแม่และตารางลูก
4. เข้าใจผลกระทบของการเลือกใช้ Constraint แต่ละแบบ

## 📁 โครงสร้างไฟล์

```
lab1/
├── README.md           # ไฟล์นี้ - คำอธิบายและคู่มือการใช้งาน
├── lab_1.md           # เนื้อหา Lab พร้อมตัวอย่าง SQL และภาพประกอบ
└── img/               # โฟลเดอร์รูปภาพประกอบผลลัพธ์
    ├── no_action*.png     # ผลลัพธ์การทดสอบ NO ACTION
    ├── set_null*.png      # ผลลัพธ์การทดสอบ SET NULL
    └── cascade*.png       # ผลลัพธ์การทดสอบ CASCADE
```

## 🚀 การใช้งาน

1. **เปิดไฟล์ [lab_1.md](lab_1.md)** เพื่ออ่านเนื้อหาและตัวอย่าง
2. **เตรียมฐานข้อมูล PostgreSQL** ให้พร้อมใช้งาน
3. **รันคำสั่ง SQL** ตามลำดับในแต่ละส่วน:
   - เตรียมสคีมาและข้อมูลเริ่มต้น
   - ทดสอบ NO ACTION
   - ทดสอบ SET NULL
   - ทดสอบ CASCADE
4. **สังเกตผลลัพธ์** และเปรียบเทียบกับภาพประกอบในโฟลเดอร์ `img/`

## 💡 สรุปสั้น Constraint แต่ละแบบ

| Constraint | ON DELETE | ON UPDATE | เมื่อใช้ |
|------------|-----------|-----------|----------|
| **NO ACTION** | ปฏิเสธ | ปฏิเสธ | ต้องการป้องกันการลบข้อมูลโดยไม่ได้ตั้งใจ |
| **SET NULL** | FK → NULL | FK → NULL | ต้องการเก็บข้อมูลลูกไว้แต่ยกเลิกการผูก |
| **CASCADE** | ลบลูกทันที | อัปเดตลูกทันที | ข้อมูลลูกไม่มีความหมายหากไม่มีแม่ |

## 📋 ตัวอย่างตาราง

Lab นี้ใช้ตารางสองตาราง:

- **dept** (ตารางแม่): เก็บข้อมูลภาควิชา
  - `did` INTEGER PRIMARY KEY
  - `dname` VARCHAR(30)

- **student** (ตารางลูก): เก็บข้อมูลนักศึกษา
  - `sid` INTEGER PRIMARY KEY
  - `sname` VARCHAR(30)
  - `gradyear` INTEGER
  - `majorid` INTEGER (FK → dept.did)

## ⚠️ ข้อควรระวัง

- ตรวจสอบให้แน่ใจว่าคอลัมน์ FK รับค่า NULL ได้หากต้องการใช้ SET NULL
- ทำงานในทรานแซกชัน (BEGIN/COMMIT/ROLLBACK) เมื่อทดสอบเพื่อป้องกันการสูญเสียข้อมูล
- สร้าง Index บนคอลัมน์ FK เพื่อประสิทธิภาพที่ดีขึ้น

## 🔗 เอกสารอ้างอิง

- [PostgreSQL Foreign Key Documentation](https://www.postgresql.org/docs/current/ddl-constraints.html#DDL-CONSTRAINTS-FK)
- [Referential Integrity Concepts](https://en.wikipedia.org/wiki/Referential_integrity)

---

**หมายเหตุ**: Lab นี้เป็นส่วนหนึ่งของวิชา Database ชั้นปีที่ 3 เทอม 2
